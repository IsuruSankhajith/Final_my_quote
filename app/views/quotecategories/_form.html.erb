<%= form_with(model: quotecategory) do |form| %>
  <% if quotecategory.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(quotecategory.errors.count, "error") %> prohibited this quotecategory from being saved:</h2>

      <ul>
        <% quotecategory.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
